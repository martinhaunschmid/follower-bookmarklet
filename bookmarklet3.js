javascript:(function a(){let followers = document.querySelectorAll("div[data-view-name='search-entity-result-universal-template']");let result = [];console.log(followers);followers.forEach(f => {let linkElem = [...f.querySelectorAll("a[href^='https:\/\/www.linkedin']")].pop();let link = linkElem.href;let imageElem = f.querySelector("img");let image = imageElem === null ? "" : imageElem.src;let name = linkElem === null ? "N/A" : linkElem.innerText;let subheadline = f.querySelector(".t-14.t-black.t-normal")?.innerText;let detailsElem = f.querySelector(".entity-result__insights div");let details = detailsElem === null ? "" : detailsElem.innerText;result.push({name: name,image: image,link: link,subheadline: subheadline,details: details});});console.log(result);scraperesult = result;tab = window.open("about:blank", "_blank");downloadLink = tab.document.createElement('a');downloadLink.setAttribute('href', 'data:application/json;charset=utf-8,' + encodeURIComponent(JSON.stringify(scraperesult)));downloadLink.setAttribute('download', 'followers.json');downloadLink.style.display = 'none';tab.document.body.appendChild(downloadLink);downloadLink.click();tab.document.body.removeChild(downloadLink);})()
